{
  "name": "kashifweb-entitlements",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "test": "vitest run",
    "deploy": "wrangler deploy",
    "migrate:remote": "wrangler d1 migrations apply kashifweb-entitlements --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260702.1",
    "typescript": "^5.7.3",
    "vitest": "^2.1.9",
    "wrangler": "^4.125.0"
  }
}
